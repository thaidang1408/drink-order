[phases.install]
cmds = [
  "npm ci --no-audit --no-fund",
  "npm ci --prefix frontend --no-audit --no-fund"
]

[phases.build]
cmds = ["DATABASE_URL=mysql://build:build@127.0.0.1:3306/build npm run build"]

[start]
cmd = "npx prisma migrate deploy && npm start"
